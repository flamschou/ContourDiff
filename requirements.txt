# ── Core training pipeline ────────────────────────────────────────────────────
# PyTorch: install separately with the right CUDA version for your GPU
# H100 (CUDA 12.x) → pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.11.0
torchvision>=0.26.0

diffusers>=0.37.0
safetensors>=0.7.0
accelerate>=1.0.0

numpy>=2.0.0
scipy>=1.17.0
Pillow>=12.0.0
h5py>=3.0.0          # required: .mat files v7.3+ (HDF5 format)
tqdm>=4.60.0
loguru>=0.7.0

# ── Translation & preprocessing scripts ──────────────────────────────────────
pandas>=2.0.0
scikit-image>=0.22.0
opencv-python>=4.8.0
